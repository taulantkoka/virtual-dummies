# Requirements for the HAPNEST GWAS benchmark (Table 1)
# Install vd_selectors first: pip install -e .

# Core
numpy>=1.22
scipy>=1.9
pandas>=1.5

# Preprocessing
pandas-plink>=2.2          # small-scale (chr 1, read_plink)
bed-reader>=0.2            # full-scale (all chr, open_bed)
joblib>=1.2                # parallel preprocessing

# Phenotype generation + benchmark worker
statsmodels>=0.13          # multipletests (BH/BY corrections)
scikit-learn>=1.1          # lars_path (BH-HD, BY-HD, Knockoffs-HD screening)

# Model-X Knockoffs competitor
knockpy>=1.3               # Spector & Janson (2022)
